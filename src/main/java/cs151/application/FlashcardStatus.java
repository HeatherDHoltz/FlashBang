package cs151.application;

/**
 * Enum representing the lifecycle/status of a flashcard.
 * Stored as the enum name in the flashcards file (FlashcardStore).
 */
public enum FlashcardStatus {
    NEW,
    LEARNING,
    MASTERED
}

